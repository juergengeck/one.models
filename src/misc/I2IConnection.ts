/*type Instance = {
    endpoints: {}
};


enum I2IConnectionState {
    Disconnected,
    Connecting,
    Connected
}

**
 * Connection between instances.
 *
class I2IConnection {

    public state: I2IConnectionState;
    private local: Instance;
    private remote: Instance;

    constructor(local: Instance, remote: Instance) {
        this.state = I2IConnectionState.Disconnected;
        this.local = local;
        this.remote = remote;
    }

    **
     * Connect the local instance with the remote instance.
     *
    connect() {
        I2IConnection
    }

}
*/
